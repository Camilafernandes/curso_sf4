{% extends 'base.html.twig' %}

{% block title %}Hello SessionController!{% endblock %}

{% block body %}
    {% if app.request.hasPreviousSession %}
        {% for message in app.session.flashBag.get('error') %}
            {{ message }}
        {% endfor %}
    {% endif %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>
    {{ app.session.remove('frase') }}
    <ul>
        <li>{{ app.session.set('frase', 'Não tem sessao') }}</li>
        <li>Your controller at <code><a href="{{ '/home/camila/Documentos/sf4/src/Controller/SessionController.php'|file_link(0) }}">src/Controller/SessionController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/home/camila/Documentos/sf4/templates/session/index.html.twig'|file_link(0) }}">templates/session/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
